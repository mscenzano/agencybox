
<div class="container">
	<div class="row">
		<div class="col-md-8">	
			<h3>Client <%= @client.name %></h3>
			<h5>Contact name <%= @client.contact_name %></h4>
			<h5>Email <%= @client.email %></h6> 
			<h5>Phone <%= @client.phone %></h6>
			<% if @projects.present? %>
			<h4>Projects:</h4> 
			<table class="table table-striped table-bordered table-hover" id="datatable-projects">
				<thead>
					<tr>
						<th>Name</th>
						<th>Description</th>
						<th>Action</th>	
					</tr>
				</thead>
				<tbody>
					<% @projects.each do |project| %>
						<tr> 
							<td><h4><%= project.name %></h4></td>
							<td><p><%= project.description[0..128] + '...' %></p></td>
							<td><%= link_to "Project details", project_path(project.id), class: 'btn btn-default btn-sm' %></td>
						</tr>
					<% end %>
				</tbody>
			</table>
			<% end %>
		</div>

	</div>
</div>
